const fs = require('fs');
const path = require('path');

module.exports = {
    port: 8888,
    sslPort: 4444,
    bindAddress: 'localhost',
    localOverrides: true,
    // target: '',
    secure: false,
    rules: [],
};
